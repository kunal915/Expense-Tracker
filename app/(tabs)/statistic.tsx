import React from "react";
import { StyleSheet, Text } from "react-native";

const Statistic = () => {
  return <Text>Statistic</Text>;
};

export default Statistic;

const styles = StyleSheet.create({});
